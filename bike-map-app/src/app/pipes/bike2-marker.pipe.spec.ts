import { Bike2MarkerPipe } from './bike2-marker.pipe';

describe('Bike2MarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new Bike2MarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
